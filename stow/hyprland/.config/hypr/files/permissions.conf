ecosystem {
	enforce_permissions = true
}

permission = /usr/(lib|libexec|lib64)/xdg-destkop-portal-hyprland, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-destkop-portal-hyprland, input-capture, allow
